apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-workload
spec:
  ingressClassName: webapprouting.kubernetes.azure.com
  tls:
    - hosts:
        - web.production.example.internal
      secretName: web-production-tls
  rules:
    - host: web.production.example.internal
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web-workload
                port:
                  number: 80
