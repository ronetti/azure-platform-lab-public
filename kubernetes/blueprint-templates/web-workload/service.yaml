apiVersion: v1
kind: Service
metadata:
  name: web-workload
  labels:
    app.kubernetes.io/name: web-workload
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: web-workload
  ports:
    - name: http
      port: 80
      targetPort: http
