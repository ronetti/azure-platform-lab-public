remote_state:
  network:
    resource_group_name: rg-tfstate-platform
    storage_account_name: sttfstateplatform
    container_name: tfstate
    key: platform/network/testing.tfstate
  shared_services:
    resource_group_name: rg-tfstate-platform
    storage_account_name: sttfstateplatform
    container_name: tfstate
    key: platform/shared-services/testing.tfstate

virtual_machines:
  - name: mgmt-01
    subnet_key: management
    size: Standard_B2ms
    private_ip: 10.10.20.10
    image:
      publisher: Canonical
      offer: 0001-com-ubuntu-server-jammy
      sku: 22_04-lts-gen2
      version: latest

configuration_management:
  inventory_contract: terraform-output
  downstream_stack: configuration-management

governance:
  single_source_of_truth: git
  change_model: pull-request
  required_reviews: 1
  error_budget_policy: review-before-vm-change
